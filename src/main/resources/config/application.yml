kafka:
  bootstrap-servers: localhost:9092
  group:
    id: test-consumer-group
  topic:
    poc: poc

rabbit:
  queue: poc

server:
  port: 0
spring:
  data:
    cassandra:
      keyspace-name: poc
#      contact-points: 192.168.107.170

